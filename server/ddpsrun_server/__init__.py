"""ddpsrun gateway server.

The package a user installs is called `ddpsrun` and is the CLI (stage 2). This
one runs in the cluster. See `docs/00-overview.md` for why the two exist.
"""

__version__ = "0.1.0"
